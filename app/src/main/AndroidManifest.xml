<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:name=".SIECAplication"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CadastrodeClientes"
        tools:targetApi="31">
        <activity
            android:name=".pressentation.FornecedoresDetail"
            android:exported="false" />
        <activity
            android:name=".pressentation.AlunosDetail"
            android:exported="false" />
        <activity
            android:name=".pressentation.Veiculos_list"
            android:exported="false" />
        <activity
            android:name=".pressentation.Fornecedores_list"
            android:exported="false" />
        <activity
            android:name=".pressentation.Alunos_list"
            android:exported="false" />
        <activity
            android:name=".pressentation.LoginUser"
            android:exported="false" />
        <activity
            android:name=".pressentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>